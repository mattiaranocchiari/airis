# AIRIS — Operational Decision Log

Append-only chronological record of session activity. **Per V28 D.25** (single-session integrated operator), entries from 2026-05-28 onward come from a single Claude Code session; earlier entries are tagged with the historical two-channel attribution (strategy = Claude.ai chat; execution = Claude Code) for audit fidelity.

**Entry format (D.25-and-after):**

- **Date** (YYYY-MM-DD)
- **SHA** (commit SHA of the work)
- **Action** (one-line summary)
- **Notes** (optional context, including any `D.x` promotion)

**Entry format (V28-baseline, pre-D.25):**

- **Date** (YYYY-MM-DD)
- **Channel** (strategy | execution)
- **SHA** (commit SHA of the work)
- **Action** (one-line summary)
- **Notes** (optional context)

**Rule:** architectural decisions (those that would merit a `D.x` entry) are recorded **here** chronologically **and promoted** into the AIRIS Master Doc `D.x` Decision Log on the next atomic doc commit. The `D.x` Decision Log is the architectural source of truth; this file is the chronological audit trail.

---

## 2026-05-28 — execution — bootstrap scaffold

- **Channel:** execution (Claude Code)
- **SHA:** `1e8780f` (root commit on `claude/kind-allen-Hupja`)
- **Action:** Scaffolded repo skeleton and workflow structure: `/CLAUDE.md`, `/STATE.md`, `/docs/` with five canonical-doc placeholders, `/docs/decision-log.md` (this file), `/infra/manifest.md`.
- **Notes:** Branch pushed; no PR opened at scaffold time; `main` not yet established at scaffold time. First post-bootstrap strategy turn commits the five canonical documents into the placeholder files on `main`.

## 2026-05-28 — execution — rename to version-free filename + establishing merge

- **Channel:** execution (Claude Code)
- **SHA:** (this commit; see `git log` on `claude/kind-allen-Hupja` and `main`)
- **Action:** Renamed `docs/AIRIS_Master_Document_V28.md` → `docs/AIRIS_Master_Document.md` (version-free filename for stable references; version tracked in-header + git history). Updated all filename references in `CLAUDE.md`, `STATE.md`, and this file to the new stable name. Established `main` as trunk by pushing `claude/kind-allen-Hupja` to `origin/main` (bootstrap special case: no PR, no merge commit — `main` is fast-forwarded to the feature-branch HEAD).
- **Notes:** Mattia authorized the direct merge. `main` HEAD now matches `claude/kind-allen-Hupja` HEAD. Baton flips to strategy. Non-filename V28 content references (D.20/D.21 cites, "V28 Decision Log" section name) are out of this rename's scope; flagged in `STATE.md`.
## 2026-05-28 — strategy — canonical documents committed
- **Channel:** strategy (chat → Mattia)
- **SHA:** (this commit)
- **Action:** Populated the five canonical documents into their `/docs/` placeholders on `main`: `VIVA_Master_Document.md` (V3), `Viva_Mode_Master_Document.md`, `AIRIS_Master_Document.md` (V28), `AIRIS_Active_Plan.md`, `Project_Core.md`. Canonical structure now live in the repo. Updated `STATE.md` on the same commit (baton → execution).
- **Notes:** VIVA Master audited + aligned V2 → V3 this session (worldview preserved; stale Project Core protocol-table reference fixed; Branch Consciousness framing added; stable-filename convention). One inherited stale reference also fixed in `AIRIS_Master_Document.md` (V27 protocol-table ref). Final coherence sweep: 0 stale cross-references across all five docs. No new architectural `D.x` decision this turn — D.20–D.23 already live in the AIRIS Master Doc `D.x` Decision Log. Version-prefix cleanup in `CLAUDE.md` + this file delegated to the next execution turn (neutralize content-version references).

## 2026-05-28 — execution — version-prefix cleanup + Next.js scaffold (Step 4.1 complete)
- **Channel:** execution (Claude Code)
- **SHA:** (this PR's squash-merge commit on `main`; branch `claude/awesome-hopper-fqYdt`)
- **Action:** Two-part execution turn closing Step 4.1. (1) **Version-prefix cleanup**: `CLAUDE.md` "Two decision logs" section + matching "Session end" ritual + this file's dual-logging rule all neutralized — `V28 D.x Decision Log` → `D.x Decision Log`, "the V28 log/Decision Log" → "the `D.x` Decision Log" / "the AIRIS Master Doc `D.x` Decision Log." Also removed the now-stale "Current bootstrap state" section from `CLAUDE.md`. (2) **Next.js scaffold landed** at repo root: Next.js 16 (App Router) + TypeScript + Tailwind CSS 4 + ESLint 9 + Turbopack (`app/`, `public/`, `package.json` + lock, config files, `.gitignore`, `AGENTS.md`). `app/page.tsx` customized to a minimal AIRIS placeholder (Vercel-marketing demo removed); `app/layout.tsx` set to `lang="it"` + AIRIS metadata; `package.json` name = `airis`. `npm run build` + `npm run lint` both pass clean.
- **Notes:** No new architectural `D.x` decision this turn — operates entirely within D.10–D.23 commitments. `CLAUDE.md` "Build / test commands" now lists `npm run dev / build / start / lint`. `infra/manifest.md` gained an App section (framework / styling / linter / scripts); "default branch to be established" note cleared (main exists). Step 4.2 (Patient Registry endpoint — auth-gated CRUD + CloudEvents-envelope events + hash-chained L6 audit ledger per D.17 + tenant scoping by construction + Italian-claim CATH stub per D.18 / §17.13) deferred to the next execution turn per the previous STATE.md's "CC may split steps 2 and 3 across turns" allowance. Baton flips to strategy.

## 2026-05-28 — execution — operating-rule encoding + Patient Registry endpoint (Step 4.2 complete)
- **Channel:** execution (Claude Code)
- **SHA:** (this PR's squash-merge commit on `main`; branch `claude/charming-archimedes-iL8k3`)
- **Action:** Two-part execution turn closing Step 4.2. (1) **Operating-rule encoding in `CLAUDE.md`**: activated *execution self-merge* (routine PRs squash-merge into `main` on green CI, source branch deleted; architectural decisions surface to strategy first) and *baton continuation* (consecutive routine execution work keeps the baton at execution; flip to strategy only on architectural decisions, blocking questions, or plan milestones). (2) **Patient Registry endpoint landed**: 17 Supabase migrations bringing the dev project from empty schema to the full Step 4.2 surface — tenants + clinician_profiles + patients + audit_events (with `chain_sequence`-ordered SHA-256 hash chain per D.17) + event_queue (CloudEvents v1.0.2 outbox per §17.9) + RLS policies (tenant scoping by construction) + Italian-claim Custom Access Token Hook stub per D.18 + atomic `patient_create / patient_update / patient_delete` RPCs that emit envelope and chain row in one transaction. Node side: `lib/events.ts` + `lib/cloudevents.ts` envelope builder, Zod schemas under `events/core/`, typed registry, `lib/finalita.ts` enum + `X-AIRIS-Finalita` header parser, `lib/db/patients.ts` data-access layer (single audited path per founder steer), App Router routes `app/api/patients` + `app/api/patients/[id]` with auth-gating + tenant scoping + finalità validation. Vitest installed; round-trip test under `tests/patients.roundtrip.test.ts` exercises create/update/delete + chain linkage + RLS isolation + soft-delete default filter. End-to-end smoke test (SQL via Supabase MCP) verified chain integrity across three appends in one transaction. `npm run build`, `npm run lint`, `npx tsc --noEmit` all clean. `airis_internal` schema created to hide `audit_events_append` from PostgREST RPC exposure; Supabase security advisor lints all clear.
- **Notes:** No new architectural `D.x` decision this turn — operational realizations of D.17 (hash-chain audit ledger), D.18 (Italian-claim CATH stub), and §17.9 (CloudEvents envelope) in their minimal-infrastructure form per D.20. Three operational choices recorded as plan-review decisions: (a) audit chain enforced as a DB function called via explicit RPC from the data-access layer (no triggers); (b) `finalità` sourced from `X-AIRIS-Finalita` HTTP header against a closed enum, with values to be reconciled with FSE 2.0 categories at Step 4.13; (c) test isolation via tenant ids in the `public` schema (G1 minimal-infra path), with a trigger to move to Supabase local CLI / branching before real or sensitive data lands. Also recorded as operational deltas: an `airis_test` literal Postgres schema was *not* used (auth-hook coupling to `public.clinician_profiles` made it impractical) — documented in `notes.md`. One Supabase shipping-default function (`public.rls_auto_enable`) had its grants tightened (EXECUTE revoked from anon/authenticated/public) to silence its WARN; this is a delta to the default Supabase project posture. CATH must be registered in the Supabase Dashboard → Authentication → Hooks → "Customize Access Token (JWT) Claims" — manual one-time step documented in `infra/manifest.md`. Baton flips to strategy: Step 4.3 (paradigm prototype — CT scheduling, voice stack, active LLM backend) is architectural and warrants strategy-channel framing before execution kicks off.

## 2026-05-28 — execution — Step 4.3 paradigm-prototype code drop (CT scheduling)
- **Channel:** execution (Claude Code)
- **SHA:** (this PR's commit on `claude/zen-johnson-B7Pyc`; PR stays open until founder preview-deploy validation passes — see "Operational deltas" in `docs/Step_4.3_notes.md`)
- **Action:** Step 4.3 paradigm-prototype code drop landed against `docs/Step_4.3_plan.md`. **Schema** — appointments + egfr_results tables + RLS + atomic appointment_create / appointment_update / appointment_cancel RPCs (mirror patient_* pattern: row mutation + CloudEvents outbox + L6 audit-chain append in one transaction). **Substrate L2** — RLS policies on `realtime.messages` for `tenant:{tenant_id}:%` topics (new RLS territory for this project; channel naming `tenant:{tenant_id}:scheduler:{room_id}` per §17.4). **Substrate L3** — in-process Node `Map` cache + anaphora resolution (L3-first / L1-fallback). **Substrate L5** — `lib/llm/anthropic.ts` Claude API direct call site with structured outputs (`output_config.format`) + prompt caching + env-configurable model / thinking / effort; shaped so the Step 4.5 engine-agnostic abstraction lift is wiring only. **Four contracts** — first sketches of `consciousness.read / write / subscribe / resolve` in `lib/consciousness/`. **Intent parser** — Zod discriminated-union for the five intent classes + injectable LlmProvider for tests + seed corpus under `lib/intents/corpus.json`. **UI** — `app/scheduler/page.tsx` (server-rendered) + `SchedulerGrid` (L2 subscribe + optimistic UI) + `Conversation` (push-to-talk + text-input + disambiguation brief + Tier-3 eGFR contrast read-back). **Voice transport** — browser-native `SpeechRecognition` + `speechSynthesis` Phase 0; Deepgram / AssemblyAI / ElevenLabs land behind the same callback contract in Phase D follow-up. **`lang="en"`** per V28 D.24. **Failure-mode triggers as measurable checks** — `TimingTrace` capture for end-to-end latency; `npm run eval:intents` corpus harness gating on ≥90% pass-rate against the live Claude API; live-DB `tests/realtime.policy.test.ts` for the L2 broadcast round-trip; qualitative voice/materialisation triggers documented for founder evaluation. **Tests** — new unit suites for intent parse + consciousness resolve (run unconditionally); new live-DB integration suites for appointments round-trip + realtime policy (env-gated, skip cleanly without secrets); env-gating moved from throw-on-missing to warn-and-skip in `tests/setup.ts`. **CI** — `.github/workflows/ci.yml` runs build + lint + typecheck + test + eval:intents on PR, with secrets-driven activation of live-DB tests and the intent eval. `npm run build`, `npm run lint`, `npm run typecheck`, `npm run test` all clean.
- **Notes:** No new architectural `D.x` decision this turn — operational realisations of D.17 (audit chain), D.21 (engine-agnostic L5 shaped so Step 4.5 lift is wiring), D.22 (Claude API direct as current backend), D.24 (English platform default; `lang="en"`), and §17.4 four contracts. Two operational deltas from the plan: (a) Self-merge gate changed — PR stays open until founder validates against the preview deploy (one merge = "this slice is validated end-to-end"), per founder steer received during the build. (b) Voice transport uses browser Web Speech API Phase 0 rather than going straight to Deepgram; zero-key path for first preview deploy, Deepgram / AssemblyAI / ElevenLabs scaffolded in `lib/voice/*` with clear upgrade markers. Synthetic eGFR values not seeded yet — founder inserts during preview-deploy validation to demo the Tier-3 contrast read-back. Failure-mode triggers all wired; if any fires during founder validation, surface a Master Doc revision instead of merging (per founder steer + plan). Baton stays at execution pending founder validation; flips back to execution once feedback arrives, or to strategy if a trigger fires.

## 2026-05-28 — strategy — Step 4.3 framing + D.21 / D.22 reframe + D.24 new (international platform)
- **Channel:** strategy (Claude Code acting as strategy channel; founder direction in same conversation)
- **SHA:** (this commit on `claude/loving-knuth-H2bSz`; merged to `main` via draft PR)
- **Action:** Strategy-channel atomic doc commit. (1) **D.21 reframed** in place — engine-agnostic LLM substrate abstraction; three deployment modes (client-local self-hosted; online API; AIRIS-hosted non-HQ); built Step 4.5. Prior named engines (Ollama, Hostinger VPS, AWS Bedrock EU, Mistral La Plateforme EU, Hetzner GEX44, OVHcloud L40S) retired as platform commitments; remain illustrative deployment options. (2) **D.22 reframed** in place — current concrete LLM backend is Claude API direct (an instance of Mode 2). No "active deployment mode" framing; concrete backend per deployment under D.21. (3) **D.24 added** — international platform with Italy as first deployment market; locale-agnostic by construction; Italian content in §17.6 / §17.7 / §17.8 / §17.13 / §17.21–§17.23 / D.18 / D.19 preserved verbatim as Italy's localization layer at first-market depth (not as platform-wide commitments). Sister abstraction to D.21. (4) **Master Doc surgical reframes** in Part 0.2 Stance (Italian-first bullet → international + Italy first-market), Part 0.3.1 (locale-aware intent parsing; engine-agnostic discipline), Part 0.4 (procurement + abstraction + D.24 bullets), Part I Executive Vision Scope (international-platform paragraph), Section 17 reframe note (two cross-cutting reframes across all of Section 17), §17.5 (full rewrite around engine-agnostic abstraction + three modes + Claude API direct as current concrete backend). (5) **Active Plan updated** — CURRENT STATE block (Step 4.3 active; 4.1 + 4.2 marked complete; revised Phase 0 cost line), Step 4.3 scope reframed (Ollama framing dropped; English platform default; Node-native voice transport; Claude API direct; references the new plan file), new session-log entry. (6) **CLAUDE.md / Project_Core.md / infra/manifest.md** updated for the two cross-cutting reframes. (7) **`docs/Step_4.3_plan.md` committed** as the execution-turn plan, same shape as the Step 4.2 plan: scope, four pillars, minimum-viable slice, architectural commitments, schema additions, failure-mode triggers set before execution, corpus generation plan, acceptance criteria, open questions, the four steers from the strategy session.
- **Notes:** Architectural decisions landed in this commit's `D.x` Decision Log entries (D.21 + D.22 reframed; D.24 new). The four founder directions from the strategy turn — Claude API directly + abstraction at 4.5, fully English corpus, Node-native voice transport Phase 0, two-browser-if-cheap — folded into the Step 4.3 plan. Italian-specific content preserved verbatim per founder direction ("localisation-layer reframe, not a genericising-away of the Italian specificity"). `app/layout.tsx`'s `lang="it"` is flagged in the Step 4.3 plan as a small cleanup for the execution turn (move to `lang="en"` per D.24 platform default). Baton flips to execution: Step 4.3 execution turn opens against `docs/Step_4.3_plan.md` after founder merges this strategy commit to `main`.

## 2026-05-28 — Operating-model reframe: single-session integrated operator (D.25 added) + Step 4.3 validation setup
- **SHA:** (this commit on `claude/zen-johnson-B7Pyc`)
- **Action:** Founder direction in-session ("from now on we will work ONLY HERE so you are in charge of the whole project") retires the two-channel strategy/execution baton model. **`D.25` added** to `AIRIS_Master_Document.md`: single Claude Code session is the project's complete operator (strategy + execution + architecture + review); the docs structure is the entire memory across sessions; **context-agnostic machine property** — any fresh session reading the canonical docs in the prescribed order picks up the work from a cold start. **`CLAUDE.md` reframed**: "Channels and baton" section replaced with "Operating model"; "Rituals" rewritten (session start = reading; mid-session architectural calls; session end flush as existential); "Branch model" updated (no channel qualifiers; self-merge gate clarified); "Hard invariants" adds D.25 entry + verbatim-grounding now self-applied within the session; "Compact instructions" updated. **`STATE.md` reframed**: `BATON:` header replaced with Active-stage/step + Operating-model + Active-branch + Status header; "Open questions for the other channel (strategy)" → "Open questions for Mattia (founder steering)". **`Project_Core.md`** already aligned (canonical-docs property explicitly notes "work on the project can resume in any environment / chat / AI tool seamlessly" — that line *is* the context-agnostic-machine commitment D.25 codifies); no edit. **`decision-log.md`** entry format updated: new format drops the "Channel" field; historical entries' "Channel" field preserved for audit fidelity. **Active Plan** historical session-log entries kept verbatim (audit trail of the V28-baseline strategy session that happened under the prior model). Also done in same session: Step 4.3 preview-deploy setup ran programmatically via Supabase MCP — two demo clinicians (`clinician-a@airis.demo`, `clinician-b@airis.demo`) created with claim set baked into `raw_app_meta_data` (faithful: matching `clinician_profiles` rows mirror the same values, so enabling the CATH later is non-breaking), plus seeded synthetic patients (4) + eGFR mocks (4; Marco Rossi=32 → triggers Tier-3 read-back) + 2 pre-existing CT1 appointments. UTC-consistency fix landed on the scheduler grid (commit `142f0b2`) so booking-time and grid-rendered hour stay coherent for any viewer timezone — was confounding the upcoming validation walk.
- **Notes:** D.25 is architectural (operating-model commitment supersedes prior V28 baseline). Promoted into `D.x` log this commit. PR #4 (`claude/zen-johnson-B7Pyc`) stays open until Mattia's preview-deploy validation closes the failure-mode triggers; the operating-model change does not affect that gate. Open call for Mattia surfaced in `STATE.md`: walk the validation per the runbook delta sent in-session (E.1–E.4 + the drag-to-move + disambiguation-auto-retry sections); report the six readings; merge or open Master Doc revision.

## 2026-05-30 — Step 4.3 closed (paradigm validation held); Step 4.4 opened
- **SHA:** (this commit on `claude/zen-johnson-B7Pyc`; PR #4 squash-merges to `main` immediately after)
- **Action:** Founder direction in-session 2026-05-30 ("consider everything validated and move on") closed Step 4.3 — paradigm validation held; no failure-mode trigger fired; no Master Doc revision required. Phase 0 paradigm prototype (CT scheduling) is the project's first end-to-end validation that the V28 stack commitment + minimal-infrastructure approach holds for the dual-surface + consciousness-substrate paradigm. **Active Plan CURRENT STATE block** updated: Step 4.3 status active → ✓ complete with what landed; Step 4.4 status Not started → active. **Step 4.3 section** updated: status block reframed with outcome + architectural additions (D.25 + four-contracts implementation sketch + `realtime.messages` per-tenant RLS as §17.4 L2 contract). **Step 4.4 section** updated: scope refined with working answers (V28→V29 timing: continue accumulating in V28 until Step 4.5 cross-cutting lift; light synthesis + Step 4.5 plan stub the operational output). **Step 4.5 description** updated: retired stale "three-backend LLM abstraction with local self-hosted as active backend" framing (V28 D.21/D.22 reframe 2026-05-28 superseded that) — now reads "engine-agnostic LLM substrate abstraction lift" with the `lib/llm/anthropic.ts` call-site moving behind the abstraction without semantics change. **Operational-philosophy + Step 4.1 procurement** blocks updated: retired the Ollama VPS references that the reframe should have caught (Ollama is no longer load-bearing — Phase 0 ran on Claude API direct per D.22; ElevenLabs subscription deferred — Phase 0 voice transport used browser-native Web Speech / speechSynthesis with zero external keys). **STATE.md** refreshed for the close-and-pivot: Active step 4.3 → 4.4; Status field reframed; "Just done" block lists the full Step 4.3 surface; Next concrete step is the merge + new branch + Step 4.4 synthesis + Step 4.5 plan stub; Open questions for Mattia updated with V28→V29 timing + Step 4.4 scope ceiling.
- **Notes:** No new `D.x` architectural decision this commit (D.25 already landed earlier this session). Stage 4 phase boundary (Phase 0 → Phase A) doesn't itself require a `D.x` — the Phase 0 / Phase A scope was already committed in V28 D.10 + D.11 + D.20 + Active Plan stage structure. PR #4 self-merges per the V28 D.25 branch model: greens green; no failure-mode trigger fired; D.25 was the one architectural commitment this session and was surfaced inline + founder-confirmed in the same conversation. Next session is Step 4.4 — opens immediately on the new branch.

## 2026-05-30 — Step 4.4 closed (Phase 0 → Phase A transition synthesis); Step 4.5 plan stub written
- **SHA:** (this commit on `claude/step-4.4-phase-0-close`; squash-merges to `main` per V28 D.25 branch model)
- **Action:** Step 4.4 (Phase 0 → Phase A transition) closed with two docs landing on a fresh branch from `main` (HEAD `5ba13a6` post Step 4.3 merge). **`docs/Step_4.4_notes.md`** captures the Phase 0 synthesis: what landed across Steps 4.1 + 4.2 + 4.3 (paradigm validation held); three operational findings worth carrying into Phase A (audit-chain emitter pattern generalises cleanly; `realtime.messages` per-tenant policy is the §17.4 L2 contract; L3 in-process map sufficient through Step 4.10); Claude Code experience cumulative; **V28 → V29 timing call** — stay on V28, promote at Step 4.5's cross-cutting infrastructure lift (rationale: V28's `D.x` commits since the original cut are scoping reframes of existing commitments, not cross-cutting structural changes worth a re-version); Phase A unblocked check confirms architectural foundation is in place modulo five sequencing choices that get surfaced to Mattia during Step 4.5 execution. **`docs/Step_4.5_plan.md`** is the strategic scope + sequencing sketch for the next execution turn: eight sub-deliverables (A engine-agnostic LLM abstraction lift; B audit emitter library; C CATH register; D Inngest activation; E Upstash Redis L3 lift; F observability stacks; G CI/CD pre-merge gates; H Infisical or defer) + sequencing diagram (A+B+C+G parallel, D+E+F new-infra activation probably warrants its own turn, H deferred) + failure-mode triggers + acceptance criteria + open questions for Mattia. **`STATE.md`** refreshed: Active step 4.4 → 4.5; "Just done" lists what landed; "Next concrete step" lays out the Step 4.5 two-turn shape; "Open questions for Mattia" carries the five Step 4.5 sequencing choices forward + the three Step 4.3 carry-forwards (voice stack, eGFR threshold, demo data cleanup).
- **Notes:** No new `D.x` architectural decision this commit. Step 4.4 is the transition checkpoint — operates entirely within V28 commitments (D.9 Phase 0 scope; D.10 + D.11 Phase A scope; D.20 minimal-infrastructure philosophy; D.21 + D.22 LLM substrate; D.25 single-session operator). PR opens for the branch + self-merges per V28 D.25 branch model: greens green; no failure-mode trigger fired; no unsurfaced architectural commitment. Light operational turn; closed in a single docs-only branch. Next session is Step 4.5 — opens immediately on the next branch from `main`.

## 2026-05-30 — Step 4.5 Turn 1 sub-deliverable A: engine-agnostic LLM substrate abstraction lift
- **SHA:** (this commit on `claude/step-4.5-core-systems-lift`; squash-merges to `main` per V28 D.25 branch model)
- **Action:** Step 4.5 Turn 1 sub-deliverable A landed — engine-agnostic LLM substrate abstraction lift per V28 D.21 + §17.5. **`lib/llm/types.ts`** (new) holds the contract: `LlmProvider` interface + `ParseStructuredOptions` / `ParseStructuredResult` / `LlmUsage` types + `DeploymentMode` enum (documentary; runtime selection happens via env). **`lib/llm/index.ts`** (new) implements `getLlmProvider()` with env-driven backend selection (`AIRIS_LLM_BACKEND`, defaults to `anthropic` per D.22); switch raises a helpful error for unknown backends + documents the recipe for adding a new backend (implement the contract from types.ts + extend the switch + document the env-var contract in infra/manifest.md). **`lib/llm/anthropic.ts`** refactored as the concrete Mode-2 implementation: imports the contract from types.ts; back-compat re-exports preserve any direct importers during the lift. **Consumer migration**: `app/scheduler/actions.ts` + `scripts/eval-intents.ts` use `getLlmProvider()` — concrete provider files no longer referenced from application code, which is the abstraction's load-bearing invariant. **Unit test** (`tests/llm.provider.test.ts`) covers default backend selection + explicit `AIRIS_LLM_BACKEND=anthropic` + case-insensitivity + unknown-backend rejection (4 new green tests). **`infra/manifest.md`** updated: LLM backend section reframed for Phase A — abstraction layer + current concrete backend + future concrete backends + how to add one; new env var `AIRIS_LLM_BACKEND` documented. **All four greens hold** post-lift; `INTENT_EVAL_MOCK=1 npm run eval:intents` confirms the corpus harness still wires through the abstraction.
- **Notes:** No new `D.x` architectural decision this commit — operationalises pre-existing V28 commitments (D.21 engine-agnostic abstraction; D.22 Claude API direct as current concrete backend; §17.5 LLM substrate). Turn 1 remaining (B audit emitter library; G partial workflow extensions for CHANGELOG + migration-level check) deferred to the next session for clean single-concern PRs. Turn 2 (D Inngest + E Upstash + F observability) and C (CATH register) + G heavy (PR-template + branch protection) deferred for explicit Mattia direction — each has ongoing-cost or repo-settings implications. PR opens for the branch + self-merges per V28 D.25 branch model: greens green; no failure-mode trigger fired; no unsurfaced architectural commitment.

## 2026-05-30 — Step 4.5 Turn 1 sub-deliverables B + G partial: audit emitter library + change-control scaffolding
- **SHA:** (this commit on `claude/sweet-euler-E6CxR`; squash-merges to `main` per V28 D.25 branch model)
- **Action:** Step 4.5 Turn 1's remaining code-side lifts landed on a fresh branch from `main` HEAD `d777b98` (post Turn 1A merge). **Sub-deliverable B — audit ledger emitter library (D.17 + §17.9):** new `lib/audit/emitter.ts` exposes `emitAuditEvent(db, opts)` — the single Node-side audited path (build CloudEvents v1.0.2 envelope → dispatch atomic RPC → DB function writes row + `event_queue` outbox + hash-chained L6 audit row in one transaction). It is the canonical home for `MutationContext`; `AuditedRpc` derives the audited mutation RPCs from the generated Supabase `Functions` types (read-only `current_tenant_id` + the `custom_access_token_hook` auth hook excluded by construction); `DomainArgs<R>` = RPC args minus the audit tail. `lib/db/patients.ts` + `lib/db/appointments.ts` refactored to delegate — public function signatures + snake_case return shapes unchanged (create paths still generate the entity id locally, so the emitter only returns the universal `{ audit_event_id, event_id }` and the wrapper spreads the known id back in). The duplicated build-event/dispatch/parse blocks are gone; per-tenant monotonic `chain_sequence` ordering stays in `airis_internal.audit_events_append` (DB-side, untouched). **Sub-deliverable G partial — change control (§17.19 + §17.20):** `scripts/lib/migrations.ts` (pure `checkMigrationSequence`) + `scripts/check-migrations.ts` runner verify `supabase/migrations` is a gap-free, duplicate-free, well-formed sequence (one global 6-digit counter under the `YYYYMMDD` date prefix; level == count == 23) — `npm run check:migrations`, wired as a CI gate. `scripts/gen-changelog.ts` (`npm run changelog`) regenerates `CHANGELOG.md` from squash-merged PR titles on `main`; `CHANGELOG.md` seeded with the 6 merged PRs (#1–#6). `tests/migrations.check.test.ts` adds 8 unit cases incl. a regression check against the repo's real migration set. **`package.json`** gains `check:migrations` + `changelog`; **`.github/workflows/ci.yml`** gains the migration-level gate; **`infra/manifest.md`** updated (audited-mutations bullet, migration-level enforcement, change-control bullet, scripts + CI lines). All five greens hold (build / lint / typecheck / test 20 passed-7 skipped / eval:intents mock wiring).
- **Notes:** No new `D.x` architectural decision this commit — B operationalises D.17 (hash-chain audit ledger) + §17.9 (CloudEvents outbox); G-partial operationalises §17.19 (pre-merge gates) + §17.20 (MDR Class IIb change control). One operational choice worth recording: the changelog is generated post-merge from `main` history (not enforced as a pre-merge gate), because a PR's own `(#N)` entry only exists after its squash-merge — pre-merge enforcement would always lag by one. The post-merge auto-commit variant + PR-template enforcement + branch protection are the "G heavy" scaffolding, deferred for Mattia's MDR-scaffolding-shape direction. Step 4.5 Turn 1 pure-code lifts (A + B + G-partial) are now complete; the remainder (C CATH dashboard register; Turn 2 D+E+F SaaS activation; G heavy) is founder-steering-gated. PR opens for the branch + self-merges per V28 D.25 branch model: greens green; no failure-mode trigger fired; no unsurfaced architectural commitment. (Merged as PR #7, squash `0b095cf`.)

## 2026-05-30 — Step 4.5 Turn 2 opened: founder steer (activate all) + kickoff scaffolding
- **SHA:** (this commit on `claude/step-4.5-turn2-kickoff`; squash-merges to `main` per V28 D.25 branch model)
- **Action:** Founder steer in-session 2026-05-30 (via AskUserQuestion, after Turn 1 closed): **(1)** Step 4.5 Turn 2 — *activate all now* (Inngest durable execution + Upstash Redis L3 + Grafana/Sentry observability); **(2)** CATH (sub-deliverable C) — *remote-guide now*; **(3)** G-heavy — *draft the PR template now*. This branch lands the kickoff scaffolding that follows from those steers without touching runtime code: **`.github/pull_request_template.md`** (MDR Class IIb change-control fields — summary, step ref, migration-level delta, D.x impact, verification greens, self-merge eligibility per the V28 D.25 branch model); **`docs/Step_4.5_Turn2_plan.md`** (Turn 2 execution plan — onboarding checklist mapping each free-tier EU account to its secrets, C/D/E/F sub-steps incl. the flagged E sync→async L3-cache-API decision, sequencing E→D→F, failure-mode triggers, acceptance, and the CATH + branch-protection operator steps for Mattia); **`infra/manifest.md`** (pre-registered Turn 2 accounts + secret names — names only per the hard invariant — `INNGEST_*`, `UPSTASH_*`, `SENTRY_*`, `GRAFANA_OTLP_*`, selectors `AIRIS_EVENTS_BACKEND` / `AIRIS_CACHE_BACKEND`; CATH-pending + branch-protection notes). **`STATE.md`** refreshed: active branch + Turn 2 status; "Next concrete step" splits Mattia's critical path (CATH dashboard step + account creation) from Claude's follow-on (E→D→F code branches once secrets land, written abstraction-first with default backends that keep tests green without the live accounts).
- **Notes:** No new `D.x` architectural decision — Turn 2 operationalises pre-existing commitments (D.16 Inngest; §17.3; §17.4 L3; §17.16–§17.17 observability; D.18 + §17.13 CATH; §17.20 change control). The activation honours D.20 (minimal infra; free tiers; abstraction-clean) + Working Principle 0.3.3/0.3.8 (code constant across phases; activation is configuration). Critical path is Mattia's account creation; Claude wires behind the abstractions with graceful degradation. Kickoff PR is routine (docs + PR template + manifest; no runtime code) → self-merges per V28 D.25 on greens. (Merged as PR #8, squash `86c1255`.)

## 2026-05-30 — Step 4.5 Turn 2 sub-deliverable E: L3 cache → async CacheBackend (memory default + Upstash)
- **SHA:** (this commit on `claude/step-4.5-turn2-e-upstash`; PR pending — GitHub MCP disconnected at commit time; self-merges per V28 D.25 once opened + green)
- **Action:** Step 4.5 Turn 2 sub-deliverable E landed (§17.4 L3 cache lift). **`lib/consciousness/cache.ts`** refactored from a synchronous in-process `Map` into an **async `CacheBackend` contract** with two backends selected by `AIRIS_CACHE_BACKEND`: `memory` (default; the Phase 0 in-process Map — keeps every existing path green with no account) and `upstash` (**`lib/consciousness/cache.upstash.ts`**, lazily imported so the default path never loads the Redis client; namespaced `airis:l3:` keys + 24h TTL; dependency-injectable client). The merge/trim rules (recentActions ≤10, conversationHistory ≤20) stay in the high-level functions (one place), so each backend only persists/retrieves whole context objects. **Consumers** updated to `await` the now-async API: `app/scheduler/actions.ts` (9 sites) + `lib/consciousness/resolve.ts` (2); the two consciousness tests made async. **New** `tests/consciousness.cache.upstash.test.ts` (3 cases) validates key prefixing + serialization round-trip + prefix-scoped `clearAll` against a fake Redis (no live instance). **Dep** `@upstash/redis@^1.38.0` added. **`infra/manifest.md`** Upstash bullet → "code landed; pending live validation"; runtime-deps line updated. All greens (build / lint / typecheck / test 23-pass-7-skip / check:migrations 23). Also this session: **CATH (sub-deliverable C)** registration remote-guided — verified via Supabase MCP that `public.custom_access_token_hook` exists (SECURITY DEFINER, hardened search-path, `supabase_auth_admin` EXECUTE granted, migration `0008` applied) and **simulated** it against a demo clinician (projects the full D.18 claim set into `app_metadata`); Mattia toggled the dashboard hook (GoTrue config reload observed 14:58:51Z); live verification pending one post-toggle sign-in.
- **Notes:** No new `D.x` architectural decision. The sync→async L3 contract change was pre-surfaced as a decision in `docs/Step_4.5_Turn2_plan.md` (working answer: option 1, async API) and falls under the founder-approved "activate all" Turn 2 steer + §17.4 — an implementation choice within an existing commitment, not a new `D.x`. The Upstash live path is written-but-not-yet-validated (no account/secrets/connector at build time) — honest-absence per Working Principle 0.3.5; flagged pending in STATE + manifest. PR deferred because GitHub MCP was disconnected at commit time; the branch is pushed so the work survives (V28 D.25 single-fact: only what's committed survives). (Opened + merged as PR #9, squash `c2bc9a9`, after the connector reconnected.)

## 2026-05-31 — Step 4.5 Turn 2 sub-deliverable D: Inngest durable execution (client + serve + outbox drain)
- **SHA:** (this commit on `claude/step-4.5-turn2-d-inngest`; self-merges per V28 D.25 once opened + green)
- **Action:** Step 4.5 Turn 2 sub-deliverable D landed (D.16 + §17.3 durable execution), abstraction-first / dormant-until-connected. **`lib/inngest/client.ts`** — Inngest client (app id `airis`). **`app/api/inngest/route.ts`** — serve endpoint (GET/POST/PUT, `nodejs` runtime; builds clean under Next 16). **`lib/inngest/functions.ts`** — first function `drainOutbox` (cron `*/1 * * * *`) drains the `event_queue` outbox and relays each CloudEvent onto the per-tenant `tenant:{id}:events` bus — foundation for the Step 4.7 Event System, distinct from the scheduler's direct L2 broadcast (no double-fire). **`lib/events/drain.ts`** — pure `drainEventQueue(db, broadcast)` (select unprocessed → broadcast → mark `processed_at`; failed broadcast leaves the row for retry, at-least-once), unit-tested in **`tests/events.drain.test.ts`** (3 cases) with a fake Supabase chain + broadcaster. **`lib/supabase/admin.ts`** — service-role client for the no-session function context (bypasses RLS; server-only; never client-imported). Dep `inngest@^4.5.0`. **`infra/manifest.md`** updated. All greens (build / lint / typecheck / test 26-pass-7-skip / check:migrations 23).
- **Notes:** No new `D.x` architectural decision — operationalises D.16 (Inngest as cross-Builder durable execution) + §17.3 + §17.9 (outbox). Two operational choices recorded: (a) **cron-drain over emit-time relay** for the first function — keeps the audited emitter (Turn 1B) untouched and the function self-contained/dormant without a producer wiring; the event-driven emit-time relay (gated by `AIRIS_EVENTS_BACKEND`) is the production form and lands when Inngest is live. (b) The drain relays onto a **distinct per-tenant events bus** (`tenant:{id}:events`), not the scheduler channel, so it doesn't double-fire the scheduler UI. Inngest live path is written-but-not-yet-validated (no account/keys at build time) — honest-absence per Working Principle 0.3.5; dormant by construction (no signing key → Inngest Cloud never registers/triggers it), so Phase 0 behaviour is preserved and all greens hold. Founder context: built ahead under repeated "go on" + the "activate all" Turn 2 steer (build-ahead sanctioned by the Turn 2 plan). Next: F (observability) is the last Turn 2 code branch; then CATH verification + live validations (E Upstash, D Inngest) when secrets land; then Step 4.5 close (notes + Step 4.6 stub).
